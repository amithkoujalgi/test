# This specifies the block bundle specs
# Uses Ubuntu as a base image

bundle:
  label: 'My ML Blocks'

  blocks:
    - block1
    - block2

  libraries:
    - pandas

  system-libraries:
    - poppler

  env:
    - envvar1: value
    - envvar2: value