# This specifies the block bundle specs
# Uses Ubuntu as a base image

name: my_blocks
version: 1.1
apt:
  - poppler
pip:
  - numpy>=2.9
  - pandas>=1.2
env:
  - envvar1: value
  - envvar2: value
images:
  - my_image:1.5